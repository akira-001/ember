diff --git a/src/skill-enhanced-proactive-agent.ts b/src/skill-enhanced-proactive-agent.ts
--- a/src/skill-enhanced-proactive-agent.ts
+++ b/src/skill-enhanced-proactive-agent.ts
@@ -64,6 +64,7 @@ export interface SkillEnhancedProactiveAgentOptions {
   slackTarget?: string;
+  mentionUserId?: string;
   systemPrompt?: string;

@@ -97,6 +98,7 @@ export class SkillEnhancedProactiveAgent implements IProactiveAgent {
   private intentionalPauseConfig: IntentionalPauseConfig;
+  private mentionUserId?: string;

   private heartbeatContext!: HeartbeatContext;

@@ -110,6 +112,7 @@ export class SkillEnhancedProactiveAgent implements IProactiveAgent {
     this.slackTarget = options.slackTarget || DEFAULT_SLACK_TARGET;
+    this.mentionUserId = options.mentionUserId;
     this.systemPrompt = options.systemPrompt || '';

+++ Add helper method after constructor (around line 125):
+
+  private withMention(text: string): string {
+    if (!this.mentionUserId) return text;
+    return `<@${this.mentionUserId}> ${text}`;
+  }

--- Apply withMention to all 4 postMessage calls:

@@ -396,7 +396,7 @@ (1st postMessage - premise)
         await this.app.client.chat.postMessage({
           channel: this.slackTarget,
-          text: staged.premise,
+          text: this.withMention(staged.premise),
         });

@@ -403,7 +403,7 @@ (2nd postMessage - main)
       const result = await this.app.client.chat.postMessage({
         channel: this.slackTarget,
-        text: staged.main,
+        text: this.withMention(staged.main),
       });

@@ -628,7 +628,7 @@ (3rd postMessage - event premise)
         await this.app.client.chat.postMessage({
           channel: this.slackTarget,
-          text: staged.premise,
+          text: this.withMention(staged.premise),
         });

@@ -635,7 +635,7 @@ (4th postMessage - event main)
       await this.app.client.chat.postMessage({
         channel: this.slackTarget,
-        text: staged.main,
+        text: this.withMention(staged.main),
       });

--- OAuth notification (line 1036) は system message なのでメンション不要

diff --git a/src/bot-instance.ts b/src/bot-instance.ts
--- a/src/bot-instance.ts
+++ b/src/bot-instance.ts
@@ Add mentionUserId when creating SkillEnhancedProactiveAgent:
       this.proactiveAgent = new SkillEnhancedProactiveAgent({
         app: this.app,
         ...
+        mentionUserId: 'U3SFGQXNH',
       });
