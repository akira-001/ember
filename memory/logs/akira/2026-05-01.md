# 2026-05-01

## セッション概要
co_view 認識精度向上のための context summary 機構（Phase 1 + 2）を実装。kotoba-whisper の構造的限界を発見し large-v3-turbo に着地、precision ~47% で実用運用開始。

午後セッションでは GitHub openagents 調査 → 共有ブラウザの仕組み（Browserbase + Playwright）解析 →「livebrowse」スキル新設 → MVP（YouTube 履歴 / Dodgers 試合 / Google Calendar）→ 教訓蓄積（pkill で Cookie 消滅、graceful close 2 段階パターン、システム日時の罠）。

---

### [MILESTONE] Phase 1: 30min Rolling Buffer + 5min Context Summary を実装
*Arousal: 0.7 | Emotion: Build*

co_view（Listening mode）認識精度向上のための補助機構を新設。`scripts/voice_chat/app.py` に以下を追加：

- `TranscriptRollingBuffer`：30 分 transcript ring（`_handle_co_view` で feed）
- `ContextSummary` (dataclass)：activity / topic / keywords / named_entities / language_register / confidence / evidence_snippets
- `_build_context_summary`：直近 30 分から gemma4 で JSON 要約
- `_summarize_context_loop`：5 分ごとに更新
- `_infer_media_content` の system prompt に `to_prompt_block()` を inject

API endpoints:
- `GET /api/context-summary` 現在値返す
- `POST /api/context-summary/feedback` Yes/No + correction 保存（`context_summary_feedback.jsonl`）

Ember Chat の Debug パネルに「推測コンテキスト」セクション + Yes/No ボタン + 修正フォーム追加。

新規 `tests/test_context_summary.py` 24 tests pass、既存 26 tests も回帰なし。

---

### [MILESTONE] Phase 2: Audio Ring + large-v3 chunk transcribe（dual-stream 設計）
*Arousal: 0.8 | Emotion: Build*

context summary 入力を「small リアルタイム結果」から「5 分ごとの large-v3 高精度チャンク」に格上げ：

- `AudioRingBuffer`：30 分 PCM リング（float32 16kHz mono）
- `TranscriptChunkBuffer`：最大 6 個の 5 分テキストチャンク
- `_chunk_transcribe_loop`：5 分間隔、lock 暴走防止スキップ付き
- `_summarize_context_loop` を改修：chunk_buffer 優先、空なら transcript_buffer フォールバック
- 永続化：`chunk_transcripts.jsonl` 追記 + `GET /api/chunk-transcripts`

クライアント側：always-on.js に**並列 MediaRecorder**を追加、30s timeslice で raw_audio_chunk WS送信。`stop/start` ローテで EBML ヘッダ問題回避。

精度向上：always-on と分離した**専用 getUserMedia（EC/NS/AGC OFF）**を新設。これにより iPad 遠距離音声の CER **80% → 47%**。

---

### [ERROR] kotoba-whisper-v2 / v1 が iPad 遠距離音声で 0 segments を返す（構造的）
*Arousal: 0.9 | Emotion: Frustration→Insight*

切替後、`segments=0 lang=ja prob=1.00 duration=*s` で空文字返却が連発。以下を順次試して全部効かず：

1. `chunk_length=15 condition_on_previous_text=False`（公式モデルカード推奨）
2. `compute_type="int8"`（CPU 標準）
3. `no_speech_threshold=0.95 log_prob_threshold=-2.0 compression_ratio_threshold=3.5` 緩和
4. `vad_filter=False`
5. PCM gain 正規化（peak→0.5）
6. kotoba-v1.0 で再試行

**Web 調査で根本原因判明**：Distil-Whisper（kotoba のベース）は anti-hallucination 訓練で「ノイズには空文字」を学習している（1% noise-only training）。kotoba-v2 訓練データは ReazonSpeech（クリーン TV 音声）+ WER>10 サンプル除外 → iPad 遠距離は完全に OOD。**仕様通りの動作**。

→ 結論：パラメータ調整では直らない。コード診断で 30 分以上溶かしたが、**Web 1 検索 5 分で解決**できた。

---

### [DECISION] STT モデルは large-v3-turbo を採用
*Arousal: 0.6 | Emotion: Resolve*

選択肢比較（2026 ベンチ + 自環境検証）：

| model | precision | speed | 構造的問題 |
|-------|-----------|-------|-----------|
| Whisper small | – | 高速 | always-on 用に維持 |
| Whisper large-v3 | 47% | 1x | 動作実績あり |
| kotoba-v2 / v1 | **0%** | 6x | distil の空文字問題で NG |
| **large-v3-turbo** | **47.6%** | **1.5x** | OK |
| ReazonSpeech NeMo | 推定 30% | GPU想定 | ランタイム入替必要 |

採用：`get_whisper()` で `large-v3-turbo`、compute_type=int8_float32、beam_size=2、vad_filter=False、3 フィルタ緩和、PCM gain 正規化。

`chunk_transcribe_interval` `context_summary_interval` を 300s → **180s（3 分）**に短縮（更新ラグ削減、kotoba 速度仮定で余裕）。

---

### [MILESTONE] アジア航測×アバント 最終報告会の文字起こし（41分）
*Arousal: 0.5 | Emotion: Done*

`/api/transcribe-file` で 41:44 の会議音声を large-v3-turbo + beam_size=5 + vad_filter=True で書き起こし。**14,058 文字 / 341 segments**。処理時間 47 分（CPU 等倍）。保存先 `scripts/ember-chat/recordings/asia_avant_meeting_20260427_121900.txt`。

固有名詞の誤変換あり（ROIC→「ロイクワック」、AS-IS→「アーズイズ」、事業ポートフォリオ→「事業ボードル」）。user-dict 追加候補。

---

### [PATTERN] 詰まったら Web Issue を先に確認
*Arousal: 0.7 | Emotion: Lesson*

外部ライブラリ・モデル特有の挙動でハマった時の鉄則：

1. 公式モデルカード / Quick Start のコード例を実装と差分比較
2. GitHub Issues で「empty / 0 segments / not working」系キーワード検索
3. HuggingFace Discussions も見る

これを**コード診断より先**にやる。kotoba 30 分溶かしたのは、HF モデルカードの「`chunk_length=15` 必須」を見ずにフィルタ緩和や PCM 統計ログを次々追加していたから。

メモリ追加：`auto-memory/feedback_check_web_issues_first.md`

---

### [INSIGHT] GitHub OpenAgents の共有ブラウザは Browserbase + Playwright
*Arousal: 0.5 | Emotion: Insight*

`openagents-org/openagents` のコード解析で判明：
- 共有ブラウザはスクラッチではなく **Browserbase（クラウド SaaS）+ Playwright（ローカル）のハイブリッド**
- Cloud モード：Browserbase の `liveUrl` を iframe で表示、Playwright が CDP 経由で操作、persistent context で認証永続
- Local モード：Playwright Chromium headless、スクショ画像で代替（ライブ感弱）
- メッセージ履歴は 3 層ストレージ（RAM 1000 件 / Hot 7 日 / Archive 180 日 gzip）
- エージェント側は MCP `workspace_get_history` 最大 500 件 + Claude Code `--resume` で session 継続

参考価値：Akiraさんの Mei + Eve + cogmem 構成は**意味記憶の質で OpenAgents を上回る**が、認証必須サイトのライブ操作は Browserbase 相当の仕組みが必要。

---

### [MILESTONE] livebrowse スキル新設（headed Chromium + CDP + 観察可能ワークフロー）
*Arousal: 0.7 | Emotion: Build*

`~/.claude/skills/livebrowse/SKILL.md` を新規作成。Akiraさんが画面を見ながら認証付き Web 調査を依頼するためのスキル。

**構成**：
- Chromium（Playwright 同梱版 1208）を `--remote-debugging-port=9222` で headed 起動
- `--user-data-dir=~/.cache/livebrowse-profiles/<name>` でプロファイル分離（google / avant / bank-* など用途別）
- Python + Playwright を CDP 経由で接続して操作
- 認証必須サイトでは Akiraさん手動ログイン → AI 抽出復帰の往復が成立

**MVP 検証**：
1. YouTube 視聴履歴 34 件抽出（2026-04 末〜2025-11、大谷/ゴルフ/キャンピングカー/AI など）
2. ドジャース 5/2 試合（vs カージナルス 09:15 JST 開始）取得
3. Google Calendar 今日の予定 5 件取得（戦略会議・月次締め・🚫振込・休日・11:00予定）
4. graceful close → 再起動 → ログイン保持 を実証

---

### [PATTERN] graceful close は 2 段階（browser.close → pgrep kill）
*Arousal: 0.9 | Emotion: Lesson*

最重要発見。`pkill -f "remote-debugging-port=9222"` で殺すと **Google 認証 Cookie（SID/HSID/__Secure-1PSID 等）がメモリから flush されずに消滅**。

**正しい手順**：
1. Playwright で `browser.close()` → CDP 切断と Cookie flush 発動
2. `pgrep -f ... | while read PID; do kill -TERM $PID; done` で残プロセス kill（既に flush 済みなので安全）

注意：
- `browser.close()` だけでは Chromium プロセスは死なない（CDP 切断のみ）
- `pgrep` 戻り値を変数経由で `kill` に渡すと zsh で複数 PID パースエラーになる（`while read` ループ必須）
- SIGKILL（`kill -9`）は flush 機会ゼロで絶対 NG

SKILL.md にコード例 + ❌ パターン明記済み。

---

### [PATTERN] system `currentDate` は信用するな、必ず `date` で実時間確認
*Arousal: 0.8 | Emotion: Lesson*

ドジャース「明日の試合」を調べる時、システム context の `currentDate: 2026-04-26` を信じて検索 → Google が「4/27 終了」と返してきて齟齬が発生。`date` で実行時刻を確認したら **JST 2026-05-01 金 07:42** だった（5 日ズレ）。

**ルール化**：
- 「今日」「明日」「昨日」を扱う前に必ず `date "+%Y-%m-%d %H:%M %A"` で実時刻取得
- 海外スポーツ等は JST 換算（PT 19:15 ≈ JST 翌朝 09:15 など）
- `datetime-awareness` スキルも併用

livebrowse SKILL.md に既知の落とし穴として記載。

---

## 引き継ぎ
- **継続テーマ（午前）**: context summary 3 分ごと更新運用、Yes/No フィードバック蓄積中、夜間学習ジョブ設計
- **継続テーマ（午後）**: livebrowse スキル運用開始。今後「live で○○」フレーズで起動。Google プロファイル（`~/.cache/yt-research-browser/`）にログイン情報あり、いずれ `~/.cache/livebrowse-profiles/google/` にリネーム検討
- **次のアクション**:
  1. 数日運用してフィードバック実データを見てから夜間学習ジョブ着手
  2. 会議録音の固有名詞誤変換（ROIC, AS-IS, 事業ポートフォリオ）を user-dict に追加
  3. 41 分音声の末尾「ご視聴ありがとうございました」ハルシネーションを手動削除推奨
  4. livebrowse プロファイルディレクトリのリネーム（任意）：`mv ~/.cache/yt-research-browser ~/.cache/livebrowse-profiles/google`
- **注意事項**:
  - kotoba-whisper 系は再採用しないこと（distil の anti-hallucination 訓練で iPad 遠距離音声に対して構造的に 0 segments）
  - livebrowse 使用時は **必ず graceful close 2 段階**（pkill 単独 NG）
  - 「今日/明日」を扱う時は **必ず `date` で実時刻確認**（system currentDate は古い）
