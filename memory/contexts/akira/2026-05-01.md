# 2026-05-01 セッションブリーフィング

## サマリー
午前: co_view 用 context summary（Phase 1+2）実装、kotoba 不採用→large-v3-turbo、precision ~47%。
午後: openagents 解析→livebrowse スキル新設、graceful close 2 段階パターン確立、YouTube/Calendar/MLB ライブ調査 MVP 成功。

## 引き継ぎ
- **継続テーマ**:
  - 3 分ごと large-v3-turbo の context summary 運用、Yes/No フィードバック蓄積→夜間学習ジョブ設計
  - livebrowse スキル運用開始（「live で○○」で起動、Google プロファイルにログイン保持済）
- **次のアクション**:
  1. 数日運用後に夜間学習ジョブ着手
  2. user-dict に ROIC / AS-IS / 事業ポートフォリオ追加
  3. livebrowse プロファイル `~/.cache/yt-research-browser` → `~/.cache/livebrowse-profiles/google` リネーム（任意）
- **注意事項**:
  - kotoba-whisper 系は再採用 NG
  - livebrowse は必ず graceful close 2 段階（pkill 単独 NG、Cookie 消滅）
  - 「今日/明日」扱う時は `date` で実時刻確認（system currentDate は古い）

## 主要決定
- STT chunk transcribe: large-v3-turbo + beam_size=2 + フィルタ緩和、interval 180s
- mic stream 分離: chunk 用 EC/NS/AGC OFF（CER 80→47%）
- livebrowse: Chromium 1208（CDP 9222）+ Playwright + プロファイル別 user-data-dir、認証は手動ログイン→AI 抽出復帰の往復モデル
