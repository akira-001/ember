# 2026-05-01 セッションブリーフィング

## サマリー
午前: co_view 用 context summary（Phase 1+2）実装、large-v3-turbo へ着地、precision ~47%。
午後: livebrowse スキル新設、graceful close 2 段階パターン確立、ライブ調査 MVP 成功。
夕方: humanness v1 三指標を実装、April 2026 ベースライン取得、`/ember-humanness` スキル新設。Eve ペルソナの構造的崩壊を発見（mean 3.84）。
夜: 旧 standalone Electron Ember Chat UI (file:// + 2501行 HTML) を React/dashboard 構成に完全復元。Always-On listening が cold start から自動復活する状態まで到達、computer-use で実機検証済み。

## 引き継ぎ
- **継続テーマ**:
  - PR #12 (`feat-dashboard-static-serve`) のマージ判定 — 差分 +3000 行
  - Eve ペルソナ修復（最優先、技術タスク時のキャラ維持を SOUL.md に）
  - context summary Yes/No フィードバック蓄積→夜間学習ジョブ設計
  - humanness v1 ベースライン取得済み: friction 24.8% / proactive engagement 1.5% / persona Mei 5.68 / Eve 3.84
- **次のアクション**:
  1. PR #12 review & merge 判定
  2. Eve ペルソナ修復: bot-conversation.ts の system prompt 構築確認
  3. `useAlwaysOn` の VAD 復活（Silero VAD 依存追加検討）
  4. proactive reaction 検出デバッグ（1.5%）
  5. humanness の cron 化
- **注意事項**:
  - Electron で web 機能を移植する際は `file://` vs `http://` origin の Chromium autoplay policy 差分を必ず確認
  - `webPreferences.webSecurity: false` はセキュリティ trade-off、信頼できる localhost dashboard 限定
  - Ember Chat.app は asar 焼き込み、変更後は `pnpm run build && cp -R "dist/mac-arm64/Ember Chat.app" /Applications/`
  - kotoba-whisper 系は再採用 NG / livebrowse は graceful close 2 段階 / 「今日/明日」は `date` で実時刻確認

## 主要決定
- 旧 Ember Chat UI (file://, 2501行) を React に 1:1 移植（12 コンポーネント新規 + scope CSS）
- Always-On は AudioContext+AnalyserNode 諦めて 5秒 chunked MediaRecorder 単独経路に
- Electron セキュリティ posture を旧版互換 (`webSecurity: false` + permission auto-grant) に戻して http:// origin の autoplay 制約を回避
- humanness v1 三指標 = friction_rate + proactive engagement + persona consistency、Ollama gemma4 限定
