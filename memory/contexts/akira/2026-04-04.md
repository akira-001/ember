# 2026-04-04 セッションブリーフィング

## サマリー
全サービス自動起動設定（Ember/cogmem/Whisper の launchd + Docker Desktop AutoStart）、ポート一覧 JSON 新規作成

## 引き継ぎ
- 継続テーマ: Ember Chat 音声体験改善、プロダクト志向の探索
- 次のアクション: 再起動後の全サービス自動起動を実機検証、Proactive ON 永続化、claude-code-slack-bot デプロイ
- 注意事項: `~/.local-apps/services.json` がポート管理マスター。ポート変更時はここを更新

## 主要決定
- 全サービス launchd で RunAtLoad + KeepAlive に統一（Docker は restart policy）
- ポート一覧を `~/.local-apps/services.json` に一元管理
