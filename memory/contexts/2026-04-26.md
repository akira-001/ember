# 2026-04-26 セッションブリーフィング

## サマリー
interest-scanner の yt_dlp ハングに wall-clock timeout を実装、運用時間を 9:00-21:00 JST に制限。scheduler-watchdog も 9:00-23:30 のみ稼働に変更。後半は PC スリープ履歴と Notification Wake の正体（macOS カレンダー / Google Calendar スヌーズ）を解析。

## 引き継ぎ
- 継続テーマ: interest-scanner と watchdog の数日挙動観察（誤アラート有無）。timeout 修正は実データで未検証（散発的バグのため）
- 次のアクション:
  1. interest-scanner / watchdog の cron-history を数日確認
  2. 必要なら Google Calendar の深夜スヌーズ通知を整理
- 注意事項: yt_dlp ハングで cron timeoutSeconds=600 を超えて19分動いた件、wall-clock timeout で予防済みだが cron runner の SIGKILL 経路は別途確認余地

## 主要決定
- interest_scanner.py の fetch_youtube に daemon thread + join(timeout=60) で全体タイムアウト追加
- cron interest-scanner: `50 * * * *` → `50 9-20 * * *`
- cron scheduler-watchdog: `*/30 * * * *` → `*/30 9-23 * * *`
