# 2026-04-26 セッションブリーフィング

## サマリー
interest-scanner の yt_dlp ハングに wall-clock timeout を実装し運用時間を 9-21JST に制限、後半は PC スリープ・Notification Wake 解析。**Session #2**: EveプロアクティブTTS「ウォーカープラス」の先頭ノイズを Irodori 確率的 lead-in と特定、`_trim_irodori_lead_in()` を実装。Ember 読み仮名追加。

## 引き継ぎ
- 継続テーマ: Irodori トリムのしきい値（threshold_rms=500 / max_trim_sec=2.0）は経験値、誤切断要観察。interest-scanner / watchdog の数日挙動も観察継続
- 次のアクション:
  1. Eveプロアクティブを数日観察、ノイズ再発と誤切断有無を確認
  2. ノイズ再発時は wav 保存して unit test に固定波形追加
  3. interest-scanner / watchdog の cron-history を数日確認
- 注意事項: TTSキャッシュは新コードでトリム後をキャッシュ。`test_yomigana_dictionary` の事前失敗（公開辞書欠落）は無関係

## 主要決定
- `scripts/voice_chat/app.py:3329` `_trim_irodori_lead_in()` 実装、`_synthesize_irodori_unlocked` の return 2箇所で適用
- `scripts/voice_chat/yomigana_map.json` に `(?i)Ember` → `エンバー` 追加
- interest_scanner.py の fetch_youtube に daemon thread + join(timeout=60) 追加
