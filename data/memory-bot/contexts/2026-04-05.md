# 2026-04-05 セッションブリーフィング

## サマリー
Ember Chat を iframe → React ネイティブ統合に完全移行。TTS品質劣化バグ（GPU並列推論）を解決。

## 引き継ぎ
- 継続テーマ: `open-claude/app.py` の `asyncio.Lock` 変更が未コミット
- 次のアクション:
  1. `open-claude` リポジトリの app.py 変更をコミット
  2. 古い whisper UI（index.html）廃止を検討（複数WS接続の根本原因）
  3. Ember Chat の実機フィードバック収集
- 注意事項: デバッグ print 文は削除済み。`_irodori_lock` のみ残存

## 主要決定
- Ember Chat iframe → React 直接統合を完了（Session 3 で方針決定 → Session 4 で実装完了）
- Irodori TTS の同時リクエスト問題は `asyncio.Lock` で直列化する方針
- `tts-quality-debug` スキルを作成（TTS品質問題の調査手順を標準化）
