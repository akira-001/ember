# 2026-04-10 セッションログ

## エントリ

- [TASK] Implicit Memory 実装完了（前セッションから継続）。ダッシュボード UI で6レイヤータブ表示、37件の事実データ移行済み確認
- [DEBUG] `ollama ps` でモデルが表示されない問題を調査。原因: `com.ollama.serve.plist` と `local.ollama.serve2.plist` の2つの serve プロセスが同時起動し、CLI が空の方に接続していた。API (`/api/ps`) では正常に表示
- [FIX] Ollama serve を `com.ollama.serve.plist` に一本化。serve2 を `.disabled` にリネーム。OLLAMA_KEEP_ALIVE=-1（常時ロード）に設定
- [DEBUG] Whisper ウェイクワード「メイ」が検出されない問題。原因: faster-whisper small モデルが先頭に「※」を付加 → `detect_wake_word` の `^` 正規表現がマッチせず ambient に流れていた
- [FIX] `wake_detect.py` に先頭記号ストリップ処理を追加（※♪★等）
- [FEATURE] カレンダー除外フィルター機能を実装。CalendarPoller が全カレンダーを取得し `excludeCalendars` で除外。ダッシュボードにトグル UI 追加。haruwa.neko を除外設定済み

## セッション概要

Ollama 二重起動問題の根本解決、Whisper ウェイクワード検出バグ修正、カレンダー除外フィルター機能の新規実装。

## 引き継ぎ
- **継続テーマ**: Implicit Memory の実運用開始（会話から嗜好・パターン等を自動学習）、カレンダーフィルターの UI 動作確認
- **次のアクション**:
  1. Bot 再起動して CalendarPoller の excludeCalendars 設定を反映
  2. ダッシュボードのカレンダー除外 UI をブラウザで視覚確認（今回ブラウザツール不安定で未確認）
  3. Ollama アプリ更新時に `com.ollama.serve.plist` の環境変数が上書きされないか監視
- **注意事項**:
  - `com.ollama.serve.plist` は Ollama.app 更新で上書きされる可能性あり（環境変数が消える）
  - wake_detect.py の記号ストリップは Whisper の誤字挿入パターンを網羅しきれていない可能性あり
