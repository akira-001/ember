# 2026-04-05 コンパクトログ
*元ログ: 2026-04-05.md | 圧縮率: 22% | 生成: 2026-04-25*

## エッセンス
Ember Chat の音声重複・Voice 保持・WS切断バグを根治し Irodori TTS を 10.5→2.3秒に高速化。iframe→React ネイティブ統合を全7タスク完了し、TTS 品質劣化対策（GPU 並列の直列化）を実施。

## 重要エントリ

### [FIX] 音声重複再生の3層問題を根治
- ポーリングをサーバー側に移動（`open-claude/scripts/voice_chat/app.py`）、クライアントポーリング全削除（`index.html`）
- `BroadcastChannel` API でタブ間排他制御（セルフループ・N^2 増殖・タブ独立再生の3層解消）

### [FIX] Voice 保持バグ（レースコンディション）
- `loadSpeakers()` がハードコードデフォルト初期化 → WS `sync_settings` が後から到着
- `GET /api/settings` 追加、`init()` で HTTP 同期取得後に保存値を渡す

### [FIX] Irodori TTS 10.5秒 → 2.3秒（4.5倍）
- `codec_device` CPU → MPS（DACVAE 4.7→0.9秒）、`num_steps` 40 → 10、`api_server.py` 更新

### [FIX] WS 即時切断バグ（プロアクティブ未再生の真因）
- `int("irodori-calm-female")` が ValueError で WS クラッシュ（app.py 2箇所）

### [DECISION] Ember Chat を iframe → React 直接統合に移行
- spec: `docs/superpowers/specs/2026-04-05-ember-chat-react-design.md`

### [MILESTONE] React ネイティブ統合 全7タスク完了
- `useEmberChat.ts` / `ChatMessages.tsx` / `ChatInput.tsx` / `ChatSettings.tsx` / `types.ts` / `EmberChatPage.tsx` / `api.ts`
- IME 確定 Enter 抑止 `keyCode !== 229`、複数タブ重複抑止 `document.visibilityState === 'visible'`

### [FIX] Last Msg「明るい女性」音質劣化（GPU 並列推論）
- 複数 WS クライアントが Irodori TTS 同時リクエスト → `asyncio.Lock` で直列化（`app.py`、`_irodori_lock`）

### [SKILL] tts-quality-debug スキル新規作成
- `~/.claude/skills/tts-quality-debug/SKILL.md`（同時リクエスト検出、パラメータ実測、非決定性切り分け）

### [PATTERN] UI フィードバック仕様の認識ズレで修正5回（ドロップダウン vs ボタン、ラベル表記等）

## 引き継ぎ
- **継続テーマ**: Ember Chat React 統合は完了。`open-claude` リポジトリの変更（`app.py` の `asyncio.Lock` 追加）は未コミット
- **次のアクション**:
  1. `open-claude/scripts/voice_chat/app.py` の `asyncio.Lock` 変更をコミット
  2. 複数タブ問題の根本対応を検討（古い `index.html` UI の廃止 or WS クライアント上限）
  3. Ember Chat の UI/UX を実機で使い込んでフィードバック収集
- **注意事項**: `open-claude` の `app.py` にデバッグ用 print 文は削除済み。`_irodori_lock` のみ残存（本番修正）
